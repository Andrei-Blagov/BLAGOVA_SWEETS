# Доступ сотрудников на Preview

| Действие | Владелец | Менеджер |
| --- | --- | --- |
| Просмотр и обработка заказов, перенос даты | Да | Да |
| Диалоги с клиентом и календарь заказов | Да | Да |
| Черновики каталога, цены, материалы базы знаний | Да | Нет |
| Публикация контента, настройки и сотрудники | Да | Нет |

Роли хранятся в `public.staff_members`. Проверка входа использует Supabase Auth и активную запись с ролью `owner` либо `manager`. Операционные RPC проверяют роль в базе; RLS скрывает материалы и неопубликованный каталог от менеджера. Интерфейс менеджера показывает только разделы «Заказы», «Диалоги», «Календарь».

Чтобы добавить менеджера на тестовом проекте:

1. Узнать рабочий email менеджера. Пароль в чат, SQL и git не записывать.
2. В Supabase Dashboard → Authentication → Users пригласить сотрудника по этому email либо проверить существующую подтверждённую учётную запись. Аккаунт создаёт и подтверждает сам сотрудник.
3. После подтверждения email администратор БД добавляет **только этого пользователя** в `public.staff_members` с ролью `manager`, `active = true`. Перед записью сверить точный email и UUID в `auth.users`; не обновлять существующую роль владельца и не повышать любого другого пользователя.
4. Проверить вход на `/login` в отдельном профиле браузера: доступны заказы, диалоги и календарь; каталог, материалы и подключения скрыты. Отзыв доступа проверять только на отдельном тестовом сотруднике внутри транзакции с `ROLLBACK` (`tests/staff-actions.sql`), не отключая рабочего менеджера.

На Preview владелец — `blagovandrey1323@gmail.com`, менеджер — `blagovasweets@gmail.com`. Обе записи подтверждены в Supabase Auth; роли `owner` и `manager` соответственно активны в `public.staff_members`. Приглашение менеджера ранее перенаправляло на localhost; Site URL Supabase Auth исправлен на `https://preview.blagovasweets.com` 24 сентября 2026 года. 24 сентября владелец сообщил об успешном восстановлении пароля и входе менеджера через `/login`. Ограничения интерфейса и отзыв доступа в отдельной сессии ещё не проверены. Не использовать общую учётную запись владельца. Настройка приглашений через интерфейс владельца и управление сотрудниками — отдельный этап.

Восстановление пароля на Preview: `/login` → «Забыли пароль?» → письмо Supabase Auth → `/reset-password`. Auth Redirect URLs разрешает точный `https://preview.blagovasweets.com/reset-password`; после сохранения пароля временная сессия закрывается и сотрудник входит через `/login`. 24 сентября обнаружен сбой: Supabase подтверждал ссылки, но страница не показывала форму. Исправление прямой отдачи callback и обработки URL fragment развёрнуто на Preview; владелец подтвердил успешное восстановление пароля 24 сентября. Старые открытые ссылки не использовать; ссылки и пароли не передавать в чат.

Проверка SQL под ролью `authenticated` и UUID менеджера подтвердила доступ к заказам и отказ в чтении каталога, цен и материалов; тестовый UUID без активной записи получил ноль строк. `supabase/tests/foundation_access.sql` и `tests/staff-actions.sql` проверяют доступ, серверные RPC и отзыв роли на временных тестовых учётных записях с `ROLLBACK`. Вход и ограничения интерфейса в отдельной браузерной сессии ещё требуют проверки на Preview.
